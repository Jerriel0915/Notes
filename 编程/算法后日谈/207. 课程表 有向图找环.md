原题链接： [207. 课程表 - 力扣（LeetCode）](https://leetcode.cn/problems/course-schedule/description/?envType=study-plan-v2&envId=top-interview-150)

---
你这个学期必须选修 `numCourses` 门课程，记为 `0` 到 `numCourses - 1` 。

在选修某些课程之前需要一些先修课程。 先修课程按数组 `prerequisites` 给出，其中 `prerequisites[i] = [ai, bi]` ，表示如果要学习课程 `ai` 则 **必须** 先学习课程  `bi` 。

- 例如，先修课程对 `[0, 1]` 表示：想要学习课程 `0` ，你需要先完成课程 `1` 。

请你判断是否可能完成所有课程的学习？如果可以，返回 `true` ；否则，返回 `false` 。

**示例 1：**
	**输入：** $numCourses = 2, prerequisites = [[1,0]]$
	**输出：**$true$
	**解释：**$总共有 2 门课程。学习课程 1 之前，你需要完成课程 0 。这是可能的。$

**示例 2：**
	**输入：**$numCourses = 2, prerequisites = [[1,0],[0,1]]$
	**输出：**$false$
	**解释：**$总共有 2 门课程。学习课程 1 之前，你需要先完成​课程 0 ；并且学习课程 0 之前，你还应先完成课程 1 。这是不可能的。$

---
总之就是给一个有向图，找是否出现环。

一开始用的[[DSU 并查集|并查集]]做的，后面发现不对，因为 DSU 表示的是集合，不能应用在这种有向图上。

### DFS 三色标记法
在 DFS 上升级改造——对每个节点，我们设有三种状态：
- 未被访问
- 正在被访问
- 已经被访问

从任意节点出发沿图递归，将一路遇到的点加入递归栈。如果发现下一个节点已经在递归栈里，则说明存在环结构。

```cpp
// Author：灵茶山艾府
class Solution {
public:
    bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
        vector<vector<int>> g(numCourses);
        for (auto& p : prerequisites) {
            g[p[1]].push_back(p[0]);
        }

        vector<int> colors(numCourses);
        // 返回 true 表示找到了环
        auto dfs = [&](this auto&& dfs, int x) -> bool {
            colors[x] = 1; // x 正在访问中
            for (int y : g[x]) {
                // 情况一：colors[y] == 1，表示发生循环依赖，找到了环
                // 情况二：colors[y] == 0，没有访问过 y，继续递归 y 获取信息
                // 情况三：colors[y] == 2，重复访问 y 只会重蹈覆辙，和之前一样无法找到环，跳过
                if (colors[y] == 1 || colors[y] == 0 && dfs(y)) {
                    return true; // 找到了环
                }
            }
            colors[x] = 2; // x 完全访问完毕，从 x 出发无法找到环
            return false; // 没有找到环
        };

        for (int i = 0; i < numCourses; i++) {
            if (colors[i] == 0 && dfs(i)) {
                return false; // 有环
            }
        }
        return true; // 没有环
    }
};
```
时间复杂度：$O(V + E)$
空间复杂度：$O(V + E)$

---

### 拓扑排序法
参考：[Kahn 算法](https://oi-wiki.org/graph/topo/#kahn-%E7%AE%97%E6%B3%95)[^1]

记录每个节点的入度，将入度为 0 的节点装入队列 $Q$。每次从 $Q$ 中弹出一个节点 $N$ 放入列表 $L$，将 $N$ 的所有邻接节点 $V$ 入度减一，将 $V$ 中所有入度为 0 的节点装入 $Q$。

循环以上过程直至 $Q$ 为空集。若 $L$ 中节点个数与图中节点数相同，则没有环；若小于原图节点数，则说明存在环。

```cpp
// Author Jerriel
class Solution {
public:
    bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
        vector<vector<int>> neighbors(numCourses);
        vector<int> degree(numCourses, 0), result;
        queue<int> check;

        // init
        for (auto& vec : prerequisites) {
            degree[vec[0]]++;
            neighbors[vec[1]].push_back(vec[0]);
        }

        for (int i = 0; i < numCourses; i++) {
            if (degree[i] == 0) {
                check.push(i);
            }
        }

        while (!check.empty()) {
            int u = check.front();
            check.pop();
            result.push_back(u);

            for (int v : neighbors[u]) {
                if (--degree[v] == 0) {
                    check.push(v);
                }
            }
        }

        return result.size() == numCourses;
    }
};
```
时间复杂度：$O(V + E)$
空间复杂度：$O(V + E)$

---
这个场景有点类似于 [[垃圾回收与内存分配#2.1.2 可达性分析|GC 的可达性分析]]。

[^1]: 该算法也应用于常见项目构建工具如 MAVEN 中的循环依赖分析，本质上也是从引用关系组成的图中找到是否存在环结构。
